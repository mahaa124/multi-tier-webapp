{
  "name": "multi-tier-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/ && cp src/app.js dist/app.js",
    "dev": "npx serve public -l 3000"
  }
}
